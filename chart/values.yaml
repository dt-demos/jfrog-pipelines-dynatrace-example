replicaCount: 1

namespace: dev

image:
  image: the_image
  tag: the_tag
  imagePullPolicy: Always

global:
  imagePullSecrets:
    - regcred       # regcred = kubectl secret name - # https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/